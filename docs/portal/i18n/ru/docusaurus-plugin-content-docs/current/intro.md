---
title: Добро пожаловать в Pali Wallet
slug: /
---

Pali Wallet — это браузерное расширение-кошелек для пользователей и приложений, которым нужен доступ и к account-based, и к UTXO-based блокчейнам из одного уровня безопасности.

Для EVM dapps Pali предоставляет MetaMask-совместимый провайдер `window.ethereum` с EIP-1193 запросами, EIP-6963 обнаружением, разрешениями аккаунтов, переключением сетей, подписанием, транзакциями и batch calls. Для Syscoin UTXO и Bitcoin-style приложений Pali предоставляет `window.pali` с методами аккаунтов, xpub, change address, подписания PSBT, транзакций и активов.

Pali также поддерживает Pali smart accounts для институций и продвинутых dapps. Dapp может попросить Pali создать и deploy smart account, поддерживаемый WebAuthn, привязать smart-account module policy во время создания, а позже выполнять атомарные batches через `wallet_sendCalls`. Восстановление существующих smart accounts выполняется в Pali.

Pali v4 перестраивает кошелек вокруг скорости и гибких полномочий подписания: batched RPC и в EVM, и в UTXO сетях для значительно более быстрого UI, smart accounts по стандартам ERC-7579 / ERC-4337 и модульная модель авторизации, в которой валидаторы (кто может подписывать) и guardians (кто может восстанавливать) — независимые, обновляемые роли на одном стабильном адресе аккаунта. Полную картину см. в [Что такое Pali?](./start-here/what-is-pali.md).

## Выберите свой путь

- **Пользователям** стоит начать с [Начало работы](./users/getting-started.md).
- **EVM-разработчикам** стоит начать с [Обнаружение провайдера](./developers/provider-discovery.md) и [Обзор EVM API](./evm-api/overview.md).
- **UTXO и Syscoin разработчикам** стоит начать с [Обзор UTXO и Syscoin API](./utxo-syscoin-api/overview.md).
- **Институциям, использующим passkeys**, стоит начать с [Passkeys и институции](./passkeys-institutions/overview.md).

## Поверхности провайдеров

| Провайдер | Семейство цепей | Основное использование |
| --- | --- | --- |
| `window.ethereum` | EVM | MetaMask-совместимые интеграции dapps, подписание, транзакции, разрешения и EIP-5792 batches. |
| `window.pali` | UTXO / Syscoin | Syscoin UTXO аккаунты, подписание PSBT, workflows с xpub/change address и helpers для активов. |

## Важная модель безопасности

Pali намеренно консервативен. Dapps подключаются отдельно для каждого хоста, блокирующие подтверждения сериализуются, несовпадения типов сетей обрабатываются явно, а пользователи подтверждают чувствительные действия в UI расширения. Может быть подключено много сайтов, но у каждого сайта одновременно есть один активный подключенный аккаунт.
