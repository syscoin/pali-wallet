import React, { FC } from 'react';

const Options: FC = () => (
  <section className="mx-auto min-w-popup h-full min-h-popup bg-bkg-2 md:max-w-2xl">
    <h1>Pali Wallet Options</h1>
  </section>
);

export default Options;
